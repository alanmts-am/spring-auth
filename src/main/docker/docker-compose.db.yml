services:
  postgres:
    image: postgres:14
    container_name: spring_auth_db
    environment:
      POSTGRES_USER: spring
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: spring-auth
    ports:
      - "5555:5432"
